{
  "welcomeFile": "/route/",
  "authenticationMethod": "none",
  "routes": [
    {
      "source": "/route/(.*)",
      "target": "$1",
      "destination": "java-service",
      "authenticationType": "xsuaa"
    }
  ]
}
